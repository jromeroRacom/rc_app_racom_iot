export interface CBSTData{
  c?: number;    // Cisterna
  t?: number;    // Tinaco
  m?: number;    // Modo
  p?: number;    // Bomba
  v?: number;    // Voltaje
  s?: number;    // Arranques
  h?: number;    // Horometro
  f?: number;    // flags de dispositivo
  lwt?: number;  // Flags de lwt
  rssi?: number; // Calidad de señal wifi
  ssid?: string; // Red wifi conectada
}
